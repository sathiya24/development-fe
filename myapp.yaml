apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-deployment
  labels:
    app: myapp-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp-deployment
  template:
    metadata:
      labels:
        app: myapp-deployment
    spec:
      containers:
      - name: myapp-deployment
        image: asia.gcr.io/devops-project-368608/development-fe-repo/myimage@sha256:7d44006b8da0088fa08412f45c57c289addf0b7ce4b8045c4106f8af2416e4b1
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: default
spec:
  ports:
  - nodePort: 30023
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: myapp-deployment
  type: NodePort
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp-ingress
  annotations:
spec:
  ingressClassName: nginx
  rules:
  - host: "myfe.sathya.shop"
    http: 
      paths: 
      - path: /
        pathType: Prefix
        backend:
          service: 
            name: myapp-service
            port:
              number: 3000
